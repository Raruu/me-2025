import { StartupAppConfig } from "@/lib/Etc/EtcStartup";

const DEFAULT: StartupAppConfig[] = [
  { appId: "me", position: "left" },
  { appId: "my-works", position: "right" },
];

export default DEFAULT;
